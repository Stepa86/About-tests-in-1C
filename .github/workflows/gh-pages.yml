name: GitHub Pages

on:
  push:
    branches:
    - master
    paths:
    - 'docs/**'

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1

    - name: Set up Python
      uses: actions/setup-python@v1
      with:
        python-version: '3.6'
        architecture: 'x64'

    - name: Install dependencies
      run: |
        pip install --upgrade pip
        pip install mkdocs mkdocs-material pymdown-extensions mkdocs-awesome-pages-plugin pygments-bsl
    
    - name: Build
      run: |
        mkdocs gh-deploy --force
        mkdocs --version
    
    - name: Public
      run: |
        cp -R temp/site/. public
    
    - name: Clean temp
      run: rm -rf temp
        
    - name: Deploy
      uses: peaceiris/actions-gh-pages@v2
      env:
        ACTIONS_DEPLOY_KEY: ${{ secrets.ACTIONS_DEPLOY_KEY }}
        PUBLISH_BRANCH: gh-pages
        PUBLISH_DIR: ./public